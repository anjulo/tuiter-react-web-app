import React from 'react';
import Logout from '../users/logout';

const Profile = () => {
  return (
    <div>
      <h1>Profile Screen</h1>
      <Logout />
    </div>
  );
}

export default Profile;